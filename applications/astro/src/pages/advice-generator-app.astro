---
import IconDice from "../assets/advice-generator-app/icon-dice.svg";
import PatternDividerMobile from "../assets/advice-generator-app/pattern-divider-mobile.svg";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta content="width=device-width" name="viewport" />
    <meta content={Astro.generator} name="generator" />
    <title>Frontend Mentors - Advice Generator App</title>
  </head>
  <body>
    <main>
      <div class="component">
        <h1>Advice #117</h1>
        <p>
          “It is easy to sit up and take notice, what's difficult is getting up
          and taking action.”
        </p>
        <img alt="" src={PatternDividerMobile.src} />
        <div>
          <img alt="" src={IconDice.src} />
        </div>
      </div>
    </main>
  </body>
</html>

<style>
  *,
  *::after,
  *::before {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  :root {
    --color-dark-blue: #202733;
    --color-dark-grayish-blue: #313a48;
    --color-neon-green: #53ffaa;
    --color-light-cyan: #cee3e9;
  }

  html {
    font-size: 10px;
    margin: 0;
    padding: 0;
  }

  body {
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: 1fr;
    height: 100vh;
    margin: 0;
    padding: 0;
  }

  main {
    align-items: center;
    background: var(--color-dark-blue, #202733);
    display: grid;
    grid-column: 1;
    grid-row: 1;
    height: 100%;
    justify-items: center;
    margin: 0;
    padding: 0;
    width: 100%;
  }

  div.component {
    align-items: center;
    background: var(--color-dark-grayish-blue, #313a48);
    border-radius: 1.5rem;
    box-shadow: 3rem 5rem 8rem 0 rgba(0, 0, 0, 0.1);
    display: flex;
    flex-direction: column;
    justify-content: center;
    margin: 0;
    max-width: 34.3rem;
    padding: 0;
    position: relative;
    width: 100%;

    & > h1 {
      color: var(--color-neon-green, #53ffaa);
      font-family: Manrope;
      font-feature-settings:
        "liga" off,
        "clig" off;
      font-size: 1.1rem;
      font-style: normal;
      font-weight: 800;
      letter-spacing: 0.3457rem;
      line-height: normal;
      margin: 4rem 2.4rem 0 2.4rem;
      padding: 0;
      text-align: center;
    }

    & > p {
      color: var(--color-light-cyan, #cee3e9);
      font-family: Manrope;
      font-feature-settings:
        "liga" off,
        "clig" off;
      font-size: 2.4rem;
      font-style: normal;
      font-weight: 800;
      letter-spacing: -0.0257rem;
      line-height: normal;
      margin: 2.4rem 2.4rem 0 2.4rem;
      padding: 0;
      text-align: center;
    }

    & > img {
      margin: 2.4rem 0 6.4rem 0;
      max-width: 29.5rem;
      padding: 0;
      width: 100%;
    }

    & > div {
      align-items: center;
      background: var(--color-neon-green, #53ffaa);
      border-radius: 6.4rem;
      bottom: 0;
      display: flex;
      height: 6.4rem;
      justify-content: center;
      left: 50%;
      position: absolute;
      transform: translate(-50%, 50%);
      width: 6.4rem;

      & > img {
        height: 2.4rem;
        width: 2.4rem;
      }
    }
  }
</style>
